import Phaser from 'phaser';
export class PlayScene extends Phaser.Scene {
  constructor() {
    super({ key: 'PlayScene' });
  }

  preload() {
    // Preload any assets needed for this scene
  }

  create() {
    // Create the game objects for this scene
  }

  update() {
    // Update the game objects for this scene
  }
}
